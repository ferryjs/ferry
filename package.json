{
  "name": "ferry",
  "version": "1.0.0",
  "description": "Ferry is (yet another) REST API framework.",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/ferryjs/ferry"
  },
  "main": "lib/Ferry",
  "scripts": {
    "test": "mocha",
    "build": "rimraf lib && babel src --out-dir lib --source-maps",
    "watch": "npm run build -- --watch",
    "prepublish": "npm run build",
    "postinstall": "npm run build"
  },
  "dependencies": {},
  "devDependencies": {
    "babel": "^4.7.12",
    "mocha": "^2.2.1",
    "rimraf": "^2.3.2",
    "sails-disk": "^0.10.7"
  }
}
